@model T.Library.Model.Slider.SaveSliderItemRequest

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>SaveSliderItemRequest</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="SliderItemSelectOption">Lựa chọn hiển thị:</label>
                <select asp-for="SliderItemSelectOption" class="form-control">
                    <option value="Newest">Sản phẩm mới nhất</option>
                    <option value="Random">Random</option>
                </select>
            </div>
            <div class="form-group">
                <label asp-for="SliderItemQuantity" class="control-label"></label>
                <input asp-for="SliderItemQuantity" class="form-control" />
                <span asp-validation-for="SliderItemQuantity" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
