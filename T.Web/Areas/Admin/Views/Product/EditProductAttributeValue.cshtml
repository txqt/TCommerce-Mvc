@model ProductAttributeMappingModel

@(await Html.PartialAsync("_AddOrUpdateProductAttributeMapping", Model))


@*<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AttributeValues.FirstOrDefault().Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AttributeValues.FirstOrDefault().WeightAdjustment)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AttributeValues.FirstOrDefault().Cost)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AttributeValues.FirstOrDefault().IsPreSelected)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AttributeValues.FirstOrDefault().DisplayOrder)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AttributeValues.FirstOrDefault().PictureId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @if (Model.AttributeValues != null)
        {
            @foreach (var item in Model.AttributeValues)
            {
                <tr>

                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.WeightAdjustment)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Cost)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.IsPreSelected)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DisplayOrder)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.PictureId)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td><p class="text-center">No data available</p></td>
            </tr>
        }
    </tbody>
</table>*@
